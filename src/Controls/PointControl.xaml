<UserControl
    x:Class="AutoCadObjectEditor.Controls.PointControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.4*" />
            <ColumnDefinition Width="0.6*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label
            Grid.Row="0"
            Grid.Column="0"
            Content="X:" />
        <TextBox
            Grid.Row="0"
            Grid.Column="1"
            Text="{Binding X, StringFormat=F4}" />

        <Label
            Grid.Row="1"
            Grid.Column="0"
            Content="Y:" />
        <TextBox
            Grid.Row="1"
            Grid.Column="1"
            Text="{Binding Y, StringFormat=F4}" />

        <Label
            Grid.Row="2"
            Grid.Column="0"
            Content="Z:" />
        <TextBox
            Grid.Row="2"
            Grid.Column="1"
            Text="{Binding Z, StringFormat=F4}" />
    </Grid>
</UserControl>
